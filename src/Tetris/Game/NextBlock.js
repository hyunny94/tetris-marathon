import React from 'react';

function NextBlock(props) {
    return (
        <React.Fragment>
            <h1> Next </h1>
            {/* <Block nextBlock={props.nextBlock} /> */}
        </React.Fragment>
    );
}

export default NextBlock;